<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.peregrinevanilla</groupId>
    <artifactId>peregrinevanilla-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Poster Theme - Parent</name>
    <description>Sports club theme (Bern Capitals lift-and-shift) for Peregrine CMS using Vue 3 Page Renderer</description>

    <modules>
        <module>core</module>
        <module>ui.apps</module>
    </modules>

    <properties>
        <!-- Build settings -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>11</java.version>

        <!-- Sling connection -->
        <sling.scheme>http</sling.scheme>
        <sling.host>localhost</sling.host>
        <sling.port>8080</sling.port>
        <sling.user>admin</sling.user>
        <sling.password>admin</sling.password>

        <!-- Dependency versions -->
        <peregrine.version>1.0-SNAPSHOT</peregrine.version>
        <sling.api.version>2.27.0</sling.api.version>
        <sling.models.version>1.3.8</sling.models.version>
        <servlet.api.version>3.1.0</servlet.api.version>
        <osgi.core.version>6.0.0</osgi.core.version>
        <osgi.annotation.version>6.0.1</osgi.annotation.version>
        <osgi.component.version>1.4.0</osgi.component.version>
        <jcr.version>2.0</jcr.version>
        <slf4j.version>1.7.6</slf4j.version>
        <jackson.version>2.12.1</jackson.version>
        <commons.lang3.version>3.12.0</commons.lang3.version>

        <!-- Plugin versions -->
        <bundle.plugin.version>5.1.2</bundle.plugin.version>
        <compiler.plugin.version>3.8.1</compiler.plugin.version>
        <resources.plugin.version>3.2.0</resources.plugin.version>
        <sling.plugin.version>2.4.2</sling.plugin.version>
        <wcmio.plugin.version>1.6.0</wcmio.plugin.version>
        <frontend.plugin.version>1.12.1</frontend.plugin.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${resources.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.felix</groupId>
                    <artifactId>maven-bundle-plugin</artifactId>
                    <version>${bundle.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.sling</groupId>
                    <artifactId>sling-maven-plugin</artifactId>
                    <version>${sling.plugin.version}</version>
                    <configuration>
                        <slingUrl>${sling.scheme}://${sling.host}:${sling.port}/system/console</slingUrl>
                        <user>${sling.user}</user>
                        <password>${sling.password}</password>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>io.wcm.maven.plugins</groupId>
                    <artifactId>wcmio-content-package-maven-plugin</artifactId>
                    <version>${wcmio.plugin.version}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <serviceURL>${sling.scheme}://${sling.host}:${sling.port}/bin/cpm/</serviceURL>
                        <userId>${sling.user}</userId>
                        <password>${sling.password}</password>
                        <failOnMissingEmbed>true</failOnMissingEmbed>
                        <force>true</force>
                        <group>peregrinevanilla</group>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>com.github.eirslett</groupId>
                    <artifactId>frontend-maven-plugin</artifactId>
                    <version>${frontend.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <dependencyManagement>
        <dependencies>
            <!-- Peregrine CMS -->
            <dependency>
                <groupId>com.peregrine-cms</groupId>
                <artifactId>base.core</artifactId>
                <version>${peregrine.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Servlet API -->
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>${servlet.api.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- OSGi -->
            <dependency>
                <groupId>org.osgi</groupId>
                <artifactId>osgi.core</artifactId>
                <version>${osgi.core.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.osgi</groupId>
                <artifactId>osgi.annotation</artifactId>
                <version>${osgi.annotation.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.osgi</groupId>
                <artifactId>org.osgi.service.component.annotations</artifactId>
                <version>${osgi.component.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- JCR -->
            <dependency>
                <groupId>javax.jcr</groupId>
                <artifactId>jcr</artifactId>
                <version>${jcr.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Sling -->
            <dependency>
                <groupId>org.apache.sling</groupId>
                <artifactId>org.apache.sling.api</artifactId>
                <version>${sling.api.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.sling</groupId>
                <artifactId>org.apache.sling.models.api</artifactId>
                <version>${sling.models.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Logging -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Jackson -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Dependency injection -->
            <dependency>
                <groupId>javax.inject</groupId>
                <artifactId>javax.inject</artifactId>
                <version>1</version>
                <scope>provided</scope>
            </dependency>

            <!-- Commons -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons.lang3.version}</version>
                <scope>provided</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>


</project>
