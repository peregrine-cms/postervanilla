<?xml version="1.0" encoding="UTF-8"?>
<workspaceFilter version="1.0">
    <!-- The theme owns and REPLACES its own namespaces on every install.
         Production content belongs in a tenant created FROM this theme. -->
    <filter root="/apps/postervanilla"/>
    <filter root="/etc/felibs/postervanilla"/>
    <filter root="/content/postervanilla"/>
</workspaceFilter>
